# Group Repo Notes

I checked the senior design GitHub organization shown on the project profile:

```text
https://github.com/accessibilitychecker25-arch
```

The current account has read-only access to the group repos, so direct pushes to the organization repos are not available from this login. The personal portfolio repo is updated instead:

```text
https://github.com/pdj0006/powerpoint-accessibility-checker
```

The group repo history shows the `Philip-Branch` branch as the best source of my contribution trail, especially around PowerPoint color contrast remediation and deployment routing fixes.
